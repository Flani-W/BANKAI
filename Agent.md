# BANKAI — built with the Multi-Agent System

This project was scaffolded using the multi-agent charter at
`C:\Users\Administrator\Documents\PROJECTS\agent\multi-agent-system`.

Roles applied (single-session, inline):
- **PM / project-orchestration** → `docs/architecture.md`, `docs/database_schema.md`,
  `contracts/api_contracts.md`, `memory/project_context.md`.
- **Frontend / ui-design-engineering** → `frontend/` (Vite + React + react-three-fiber).
- **Backend / backend-engineering** → `backend/` (Express + MongoDB, login only).

See `memory/project_context.md` for the live decisions log and `README.md` to run it.
